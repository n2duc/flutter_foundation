import 'package:flutter/material.dart';

class CartOption extends StatelessWidget {
  const CartOption({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
